❌ Search ranking — confirmed. The search just filters snippets by string match, no score/relevance/recency weighting. Results come back in whatever order the store has them.
❌ Backlinks panel — no "notes that link to this one" panel anywhere
❌ Note templates — no template system
❌ CI pipeline — no GitHub Actions